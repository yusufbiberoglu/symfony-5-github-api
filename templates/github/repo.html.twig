<a href="{{ repo.url }}">{{ repo.name }}</a>
<br />
<span class="small">{{ repo.description }}</span>
<span class="pull-right">
    {{ repo.stargazers_count }} <i class="glyphicon glyphicon-star"></i>
</span>
And lastly the index template:

<!-- app/Resources/views/githut/index.html.twig -->

{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-sm-3">
        {{ render (controller('App\\Controller\\GithubController::profile',, { 'username': username })) }}
    </div>

    <div class="col-sm-9">
        {{ render (controller('App\\Controller\\GithubController::repos', { 'username': username })) }}
    </div>
{% endblock %}
